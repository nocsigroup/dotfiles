#!/usr/bin/env zsh

function cargo-clippy {
    local bindir="/Users/locnguyen/.zinit/plugins/rust/bin"
    
    
    local -xU PATH="$bindir":"$PATH"
    
    "$bindir"/"cargo-clippy" "$@"

}

cargo-clippy "$@"
